<!DOCTYPE html>
<html>
<head>
    <title>🌱 AI Smart Irrigation Dashboard</title>
    <meta name="viewport" content="width=device-width">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <div class="container">
        <!-- HEADER -->
        <header>
            <h1>🌾 AI Smart Irrigation System</h1>
            <div class="time" id="current-time"></div>
        </header>

        <div class="dashboard">
            <!-- SENSORS -->
            <div class="cards">
                <div class="card moisture">
                    <h3>💧 Soil Moisture</h3>
                    <div class="value" id="moisture-val">45%</div>
                    <div class="bar"><div class="fill" id="moisture-bar"></div></div>
                    <span class="status" id="moisture-status">Optimal</span>
                </div>
                
                <div class="card temp">
                    <h3>🌡️ Temperature</h3>
                    <div class="value" id="temp-val">28.5°C</div>
                    <div class="bar"><div class="fill temp-fill" id="temp-bar"></div></div>
                </div>
                
                <div class="card humid">
                    <h3>💨 Humidity</h3>
                    <div class="value" id="humid-val">65%</div>
                    <div class="bar"><div class="fill humid-fill" id="humid-bar"></div></div>
                </div>
            </div>

            <!-- PUMP CONTROL -->
            <div class="card pump-control">
                <h3>🚿 Pump Status</h3>
                <div class="pump-circle" id="pump-circle">
                    <span id="pump-text">OFF</span>
                </div>
                <div class="pump-buttons">
                    <button onclick="pumpOn()" class="btn on">TURN ON</button>
                    <button onclick="pumpOff()" class="btn off">TURN OFF</button>
                </div>
                <div id="last-water">Last watered: Never</div>
            </div>

            <!-- CHART -->
            <div class="card chart-card">
                <h3>📊 Live Trends</h3>
                <canvas id="chart"></canvas>
            </div>

            <!-- WEATHER & ALERTS -->
            <div class="card weather-card">
                <h3>🌤️ Weather & Alerts</h3>
                <div class="weather-info">
                    <div>🌧️ Rain Chance: <span id="rain-val">10%</span></div>
                    <div>⏰ Next Action: <span id="next-action">Monitor</span></div>
                </div>
                <div class="alert" id="alert">✅ System Optimal</div>
            </div>
        </div>
    </div>

    <script src="/static/dashboard.js"></script>
</body>
</html>